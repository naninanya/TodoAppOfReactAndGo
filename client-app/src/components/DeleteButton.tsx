const DeleteButton = () => {
    return (
        <button className="DeleteButton" type="submit">
            Clear completed Todo items
        </button>
    );
};

export default DeleteButton